import javax.swing.*;

public class MyButton extends JButton {

    MyButton(String text){
        super(text, null);
        //setDefaultCloseOperation(EXIT_ON_CLOSE);
        setSize(200, 100);
    }
}
